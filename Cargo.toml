[package]
name = "subrapid"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
openssl = { version = "0.10.75", features = ["vendored"] }
reqwest = { version = "0.12.24", features = ["blocking"] }
